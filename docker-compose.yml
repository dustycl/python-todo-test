services:
  web:
    build: .
    ports:
      - "8000:8000"
    environment:
      - SECRET_KEY=${SECRET_KEY:?Set SECRET_KEY in .env or environment}
      - DATABASE=/app/instance/todo.db
    volumes:
      - todo-data:/app/instance

volumes:
  todo-data:
